import { Observable } from 'rxjs';
declare const calculateSubscriptionFrame: (observable: Observable<any>, unsubscriptionMarbles: string | null, frameTimeFactor: number) => {
    subscribedFrame: number;
    unsubscribedFrame: number;
};
export { calculateSubscriptionFrame };
